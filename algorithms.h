#include "memoization.h"

class Algorithms {
public:
    static unsigned long long fiboRecursive(unsigned long long n);
    static unsigned long long fiboIterative(unsigned long long n);
    static unsigned long long fiboIterativeMemoized(unsigned long long n);
};